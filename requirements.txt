# --- Offline core (evaluation, demo, tests) ---
# NONE required. The schema, windowing, parsing, evaluation, demo and tests run
# on the Python standard library alone.

# --- Only needed when running against real models on the server ---
google-genai>=0.3     # golden labeler: Gemini 3 Pro (audio + transcript)
openai>=1.30          # candidate labeler: Qwen via any OpenAI-compatible endpoint
PyYAML>=6.0           # config loading (src/config.py)

# --- Candidate via DOWNLOADED weights (server GPU; scripts/02 --backend local) ---
# Swap models by passing a different --model <hf_id>. Install ONE engine:
# vllm>=0.6                       # fast, batched (default engine)
# transformers>=4.44             # universal fallback (--engine hf)
# torch                          # required by both — install the CUDA build for your GPU
# accelerate                     # device_map for the hf engine

# --- Or self-host behind an OpenAI-compatible server (scripts/02 --backend endpoint) ---
# vllm>=0.6                       # `vllm serve Qwen/Qwen3-32B` then point --base-url at it
