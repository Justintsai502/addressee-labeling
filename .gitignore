__pycache__/
*.pyc
config.yaml
# outputs/ holds the committed "played-model" demo labels + report (see README §5).
# Real server runs overwrite these; commit or ignore them as you prefer.
data/**/audio/
*.wav
*.flac
# AMI corpus data is large and license-restricted — never commit it. Regenerate
# with scripts/00_prepare_ami.py after downloading (see data/README.md).
data/ami/
.env
.venv/

# labels pulled down from the server with rsync (not source)
outputs_server/
